{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "npm run test:watch",
            "name": "Test with watch mode",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}